# S3 / CDN để publish bản build (dùng bởi scripts/publish.mjs).
# Copy file này thành .env và điền giá trị thật. .env KHÔNG commit.
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=HAN02
AWS_BUCKET=
AWS_URL=https://han02.vstorage.vngcloud.vn
AWS_ROOT_FOLDER=shipping-api
CDN_URL=
